# Server Configuration
PORT=3000
NODE_ENV=development

# API Security
# Master key has full access to all apps and can manage API keys
MASTER_KEY=your-master-key-change-me-in-production

# Database Configuration
# SQLite (default)
DATABASE_URL=sqlite://./data/metrics.db

# PostgreSQL
# DATABASE_URL=postgres://user:password@localhost:5432/metrics

# MySQL
# DATABASE_URL=mysql://user:password@localhost:3306/metrics

# Data Retention
RETENTION_DAYS=30

# White-Label / Branding (Optional)
# Customize the dashboard appearance and branding
APP_NAME=Acme Metrics
APP_TAGLINE=Real-time Application & Infrastructure Monitoring
PRIMARY_COLOR=#6366f1
LOGO_URL=https://via.placeholder.com/150x40/6366f1/ffffff?text=ACME
FAVICON_URL=https://via.placeholder.com/32/6366f1/ffffff?text=A
FOOTER_TEXT=© 2025 Acme Corporation. All rights reserved.

# AWS Integration (Optional)
# Configure per-app using POST /api/aws/config endpoint
# The server will use AWS STS to assume roles configured per app
# This allows forwarding metrics to CloudWatch and alerts to SNS
# Example configuration via API:
# curl -X POST http://localhost:3000/api/aws/config \
#   -H "X-API-Key: your-api-key" \
#   -H "X-App-Id: my-app" \
#   -H "Content-Type: application/json" \
#   -d '{"roleArn":"arn:aws:iam::123456789012:role/MetricCollectorRole","region":"us-east-1"}'
